# Следующие шаги для интеграции Otterbrix в JSONBench

## 🎯 Немедленные действия

### 1. Тестирование Python API Otterbrix (1-2 часа)

Создайте и запустите тестовый скрипт для проверки возможностей Otterbrix:

```bash
cd /home/tolisso/otterbrix/for_ai/JSONBench
# Создать тестовый скрипт из 02_OTTERBRIX_API_REFERENCE.md
python3 test_syntax.py
```

**Цель**: Определить точный синтаксис для:
- Доступа к JSON-полям
- Работы с timestamp
- Агрегатных функций
- Приведения типов

### 2. Создание прототипа (2-3 часа)

Создайте минимальный работающий прототип:

```bash
cd /home/tolisso/otterbrix/JSONBench
mkdir otterbrix
cd otterbrix

# Создать простой тестовый скрипт
cat > test_prototype.py << 'EOF'
#!/usr/bin/env python3
from otterbrix import Client
import json
import gzip

# 1. Создать клиента
client = Client()

# 2. Создать БД и таблицу
client.execute("CREATE DATABASE IF NOT EXISTS test_bluesky")
client.execute("USE test_bluesky")
client.execute("CREATE TABLE IF NOT EXISTS bluesky (data JSON)")

# 3. Загрузить несколько записей
test_file = "/home/tolisso/data/bluesky/file_0001.json.gz"

count = 0
with gzip.open(test_file, 'rt') as f:
    for line in f:
        if count >= 1000:  # Только 1000 записей для теста
            break
        try:
            doc = json.loads(line)
            json_str = json.dumps(doc).replace("'", "''")
            client.execute(f"INSERT INTO bluesky VALUES ('{json_str}')")
            count += 1
            if count % 100 == 0:
                print(f"Loaded {count} records...")
        except Exception as e:
            print(f"Error: {e}")
            continue

print(f"Total loaded: {count} records")

# 4. Проверить запросы
queries = [
    "SELECT COUNT(*) FROM bluesky",
    "SELECT data.commit.collection, COUNT(*) FROM bluesky GROUP BY data.commit.collection LIMIT 5",
]

for q in queries:
    print(f"\nQuery: {q}")
    try:
        cursor = client.execute(q)
        results = cursor.fetchall()
        for row in results:
            print(f"  {row}")
        cursor.close()
    except Exception as e:
        print(f"  Error: {e}")

# 5. Очистка
client.execute("DROP DATABASE test_bluesky")
EOF

chmod +x test_prototype.py
python3 test_prototype.py
```

**Ожидаемый результат**: 
- 1000 записей загружены
- Запросы выполнены успешно
- Синтаксис определен

### 3. Адаптация запросов (2-3 часа)

Основываясь на результатах прототипа, создайте `queries.sql`:

```bash
cd /home/tolisso/otterbrix/JSONBench/otterbrix

# Скопировать шаблон из 03_IMPLEMENTATION_GUIDE.md
# Адаптировать под найденный синтаксис
nano queries.sql
```

**Проверка каждого запроса**:
```python
# Создать test_queries.py
for query in queries:
    print(f"Testing: {query[:50]}...")
    cursor = client.execute(query)
    result = cursor.fetchall()
    print(f"  Rows: {len(result)}")
    cursor.close()
```

## 📅 Недельный план

### День 1: Подготовка и тестирование API

**Утро (2-3 часа):**
- ✅ Прочитать всю документацию в `/for_ai/JSONBench/`
- ✅ Установить и протестировать Otterbrix
- ✅ Запустить `test_syntax.py`

**День (3-4 часа):**
- ✅ Создать и запустить прототип
- ✅ Определить синтаксис для всех операций
- ✅ Задокументировать найденный синтаксис

**Вечер (1-2 часа):**
- ✅ Адаптировать 5 запросов JSONBench
- ✅ Протестировать каждый запрос на 1000 записей

### День 2-3: Реализация скриптов

**Приоритет 1 (критически важные):**
- `install.sh` - установка Otterbrix
- `ddl.sql` - создание таблицы
- `queries.sql` - 5 запросов (уже готово)
- `load_data.sh` - загрузка данных

**Приоритет 2 (важные):**
- `run_queries.sh` - выполнение запросов с таймингом
- `benchmark.sh` - обработка результатов
- `main.sh` - оркестрация

**Приоритет 3 (дополнительные):**
- `count.sh`, `total_size.sh`, `drop_table.sh`
- `create_and_load.sh`, `uninstall.sh`

### День 4: Тестирование на 1M

**План:**
1. Скачать 1M датасет (если еще не скачан)
2. Запустить `./main.sh 1`
3. Отладить проблемы
4. Проверить результаты
5. Сравнить с DuckDB

**Целевые метрики:**
- Загрузка < 5 минут
- Каждый запрос < 10 секунд
- Все запросы корректны

### День 5-6: Оптимизация и тестирование 10M

**Оптимизация:**
- Увеличить размер batch
- Добавить индексы (если помогает)
- Оптимизировать запросы

**Тестирование 10M:**
- Запустить `./main.sh 2`
- Целевые метрики:
  - Загрузка < 30 минут
  - Каждый запрос < 1 минуты

### День 7: Документация

**Создать:**
- `README.md` в `otterbrix/`
- Описание особенностей
- Инструкции по запуску
- Известные ограничения

**Подготовить:**
- Структуру для results/
- Шаблоны JSON-файлов

## 🚀 Запуск на AWS (после успешного тестирования)

### Шаг 1: Создание инстанса

```bash
# AWS m6i.8xlarge
# - 32 vCPU
# - 128 GB RAM
# - 10 TB gp3 disk
# - Ubuntu 24.04
```

### Шаг 2: Установка зависимостей

```bash
# На инстансе
sudo apt update
sudo apt install -y python3 python3-pip git

# Клонировать JSONBench
git clone https://github.com/ClickHouse/JSONBench.git
cd JSONBench

# Скопировать директорию otterbrix
scp -r /home/tolisso/otterbrix/JSONBench/otterbrix ubuntu@instance:/home/ubuntu/JSONBench/
```

### Шаг 3: Скачать полный датасет

```bash
cd JSONBench
./download_data.sh
# Выбрать 4 (1000m)
# Время: несколько часов
```

### Шаг 4: Запустить бенчмарк

```bash
cd otterbrix
./main.sh 5  # Все размеры: 1m, 10m, 100m, 1000m
```

**Мониторинг:**
```bash
# В отдельной сессии
watch -n 5 'ps aux | grep python; df -h; free -h'
```

### Шаг 5: Сбор результатов

```bash
# Скопировать результаты обратно
scp ubuntu@instance:/home/ubuntu/JSONBench/otterbrix/*.results_runtime ./
scp ubuntu@instance:/home/ubuntu/JSONBench/otterbrix/*.data_size ./
scp ubuntu@instance:/home/ubuntu/JSONBench/otterbrix/*.count ./
```

## 📋 Краткий чек-лист на каждый день

### Каждое утро
- [ ] Проверить, что задачи на день ясны
- [ ] Обновить чек-лист (`05_CHECKLIST.md`)
- [ ] Запустить тестовое окружение

### В течение дня
- [ ] Документировать все проблемы и решения
- [ ] Делать коммиты изменений
- [ ] Тестировать после каждого изменения

### Каждый вечер
- [ ] Обновить прогресс в `05_CHECKLIST.md`
- [ ] Задокументировать результаты дня
- [ ] Запланировать задачи на завтра

## 🔍 Отладка проблем

### Если SQL синтаксис не работает

```python
# Проверить все варианты из 04_QUERY_TRANSLATION.md
syntaxes = [
    ("Dot", "SELECT data.did FROM bluesky"),
    ("Arrow", "SELECT data->'did' FROM bluesky"),
    ("Path", "SELECT data->>'$.did' FROM bluesky"),
    ("Function", "SELECT JSON_EXTRACT(data, '$.did') FROM bluesky"),
]

for name, query in syntaxes:
    try:
        cursor = client.execute(query)
        print(f"✓ {name} works")
        cursor.close()
    except Exception as e:
        print(f"✗ {name}: {e}")
```

### Если загрузка медленная

**Попробуйте:**
1. Увеличить размер batch (10000 → 50000)
2. Использовать bulk insert
3. Отключить индексы
4. Использовать несколько потоков

### Если Out of Memory

**Решения:**
1. Уменьшить batch size
2. Закрывать курсоры
3. Периодически вызывать garbage collector
4. Проверить утечки памяти

## 📞 Получение помощи

### Внутренние ресурсы
1. Документация в `/for_ai/JSONBench/`
2. Примеры в `JSONBench/duckdb/`, `JSONBench/postgresql/`
3. Команда Otterbrix: team@otterbrix.com

### Внешние ресурсы
1. JSONBench GitHub Issues
2. Otterbrix GitHub Issues
3. Документация Otterbrix

## 🎯 Критерии готовности к PR

Перед созданием PR убедитесь:

- [ ] Все скрипты работают без ошибок
- [ ] Результаты на 1M проверены и корректны
- [ ] Результаты на 10M получены
- [ ] Результаты на 100M и 1000M получены (для полного PR)
- [ ] README.md написан
- [ ] Все особенности задокументированы
- [ ] Код чистый и понятный
- [ ] Нет захардкоженных путей
- [ ] Следование принципам JSONBench

## ✨ После завершения

### Публикация результатов
1. Создать PR в JSONBench
2. Написать blog post о результатах
3. Обновить документацию Otterbrix
4. Поделиться результатами в соцсетях

### Оптимизация (опционально)
1. Проанализировать узкие места
2. Улучшить производительность
3. Запустить бенчмарк повторно
4. Обновить результаты

---

**Начните с шага 1 (Тестирование API) сегодня!**

Удачи с интеграцией! 🚀

