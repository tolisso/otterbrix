# JSONBench: Финальный анализ document vs document_table (ИСПРАВЛЕНО)

## Дата: 08.12.2025
## Датасет: 1000 JSON записей (Bluesky data)

---

## 🔍 Обнаруженная проблема и решение

### Проблема
При вставке JSON документов **без явного поля `_id`**, document storage не генерировал уникальные ID автоматически.

**Причина:**
```cpp
// components/document/document.cpp:1009
document_id_t get_document_id(const document_ptr& doc) { 
    return document_id_t{doc->get_string("/_id")}; 
}
```

Функция `get_document_id()` просто **читает** `/_id` из документа, но **НЕ генерирует** его!

Когда `/_id` отсутствует:
- `doc->get_string("/_id")` возвращает пустую строку или default значение
- Все документы получают **одинаковый** document_id
- В B-tree все записи **затирают друг друга** (последняя вставка побеждает)
- Результат: вместо 1000 записей сохраняется только **1 запись**

### Решение
Добавили генерацию уникального `_id` **перед вставкой** в document storage:

```cpp
// Add unique _id if missing (required for B-tree documents storage)
if (!doc->is_exists("/_id")) {
    std::ostringstream id_stream;
    id_stream << std::setfill('0') << std::setw(24) << doc_idx;
    doc->set("/_id", id_stream.str());
}
```

**Почему document_table не имел этой проблемы?**
- document_table использует свой собственный механизм генерации row_id
- Не полагается на `_id` из документа для первичного ключа
- Хранит маппинг `id_to_row_` отдельно от данных

---

## 📊 Финальные результаты (КОРРЕКТНЫЕ)

### Test 1: INSERT Performance

```
╔══════════════════════════════════════════════════════════════╗
║                    INSERT COMPARISON                         ║
╠══════════════════════════════════════════════════════════════╣
║ document_table:    4795 ms  (  208.6 rec/s)                 ║
║ document:            97 ms  (10309.3 rec/s)                 ║
╠══════════════════════════════════════════════════════════════╣
║ Winner: document (B-tree) - 49.4x faster                    ║
╚══════════════════════════════════════════════════════════════╝
```

**Анализ:**
- ⚡ **document (B-tree): 49.4x быстрее** для INSERT
- Row-oriented формат: документ вставляется целиком в B-tree узел
- document_table медленнее из-за:
  - Парсинг всех JSON paths (глубокий обход структуры)
  - Эволюция динамической схемы
  - Преобразование в колоночный формат
  - Создание/обновление индексов колонок

### Test 2: SELECT * Performance

```
╔══════════════════════════════════════════════════════════════╗
║                  SELECT * COMPARISON                         ║
╠══════════════════════════════════════════════════════════════╣
║ document_table:      95 ms  (1000 records) ✅               ║
║ document:             1 ms  (1000 records) ✅               ║
╠══════════════════════════════════════════════════════════════╣
║ Winner: document (B-tree) - 95.0x faster                    ║
╚══════════════════════════════════════════════════════════════╝
```

**Анализ:**
- ✅ Обе системы **корректно** возвращают все 1000 записей
- ⚡ **document (B-tree): 95.0x быстрее**
- B-tree хранит документы в готовом виде → просто читает и отдает
- document_table должен реконструировать JSON из колонок:
  - Читает все колонки для каждой строки
  - Собирает обратно в документ
  - Более затратная операция

### Test 3: SELECT WHERE Performance

```
╔══════════════════════════════════════════════════════════════╗
║               SELECT WHERE COMPARISON                        ║
╠══════════════════════════════════════════════════════════════╣
║ document_table:     108 ms  (990 records found) ✅          ║
║ document:             2 ms  (990 records found) ✅          ║
╠══════════════════════════════════════════════════════════════╣
║ Winner: document (B-tree) - 54.0x faster                    ║
╚══════════════════════════════════════════════════════════════╝
```

**Анализ:**
- ✅ Обе системы **корректно** находят 990 записей с `kind="commit"`
- ⚡ **document (B-tree): 54.0x быстрее**
- Обе системы делают full scan (нет индексов на поле `kind`)
- document_table медленнее из-за:
  - Дополнительные накладные расходы на колоночный формат
  - Реконструкция документов из колонок

---

## 🎯 Итоговая сравнительная таблица (КОРРЕКТНАЯ)

| Операция | document (B-tree) | document_table | Преимущество |
|----------|-------------------|----------------|--------------|
| **INSERT 1K** | 97ms (10,309 rec/s) ⚡ | 4,795ms (209 rec/s) | **document 49.4x** |
| **SELECT *** | 1ms (1000 records) ⚡ | 95ms (1000 records) | **document 95.0x** |
| **SELECT WHERE** | 2ms (990 records) ⚡ | 108ms (990 records) | **document 54.0x** |
| **Корректность** | ✅ (с явным _id) | ✅ (всегда) | **document_table** |
| **Простота API** | ⚠️ требует _id | ✅ автоматический | **document_table** |

---

## 🔬 Детальный анализ производительности

### Почему document (B-tree) быстрее?

#### 1. INSERT (49.4x быстрее)
- **Row-oriented storage**: документ как единое целое
- Минимальный парсинг (только для _id)
- Одна операция B-tree insert
- Нет преобразования формата

vs

- **Column-oriented storage**: разбор всех полей
- Полный JSON parsing для извлечения paths
- Эволюция схемы при новых полях
- Множественные операции вставки (по колонке)

#### 2. SELECT * (95x быстрее)
- **Direct read**: документы уже в JSON формате
- B-tree in-order traversal
- Минимальная сериализация

vs

- **Reconstruction**: сборка документа из колонок
- Чтение множественных векторов
- Конвертация каждого значения
- Создание JSON структуры

#### 3. SELECT WHERE (54x быстрее)
- **Документ целиком в памяти**: быстрая фильтрация
- Sequential scan по документам
- Прямой доступ к полям

vs

- **Колоночное чтение**: дополнительная индирекция
- Реконструкция для проверки условий
- Overhead колоночного формата

### Когда document_table будет быстрее?

1. **Проекция малого числа колонок**:
   ```sql
   SELECT did, kind FROM records;  -- читаем только 2 колонки из 50+
   ```
   - document: читает весь документ
   - document_table: читает только 2 вектора
   - **Потенциал: 10-20x ускорение**

2. **Агрегации по колонкам**:
   ```sql
   SELECT COUNT(*), kind FROM records GROUP BY kind;
   ```
   - document: full scan документов + парсинг kind
   - document_table: sequential read одного вектора
   - **Потенциал: 5-10x ускорение**

3. **Фильтрация с проекцией**:
   ```sql
   SELECT did FROM records WHERE kind = 'commit';
   ```
   - document: читает весь документ, фильтрует, проецирует
   - document_table: фильтрует по kind вектору, читает только did
   - **Потенциал: 3-5x ускорение**

4. **Аналитические запросы**:
   ```sql
   SELECT AVG(LENGTH(text)) FROM records WHERE kind = 'post';
   ```
   - Колоночный формат идеален для таких запросов
   - **Потенциал: 20-50x ускорение**

---

## 💡 Выводы и рекомендации

### Для текущего состояния (1K records)

**document (B-tree)** побеждает по всем показателям:
- ✅ **49.4x быстрее** INSERT
- ✅ **95.0x быстрее** SELECT *
- ✅ **54.0x быстрее** SELECT WHERE
- ⚠️ Требует явную генерацию `_id`

**document_table** имеет преимущества:
- ✅ **Надежность**: автоматическая генерация ID
- ✅ **Гибкая схема**: автоматическая адаптация
- ✅ **O(1) primary key lookup**: с нашим primary_key_scan
- ❌ **Производительность**: пока медленнее на simple queries

### Рекомендации по использованию

#### Используйте **document (B-tree)** для:
1. **OLTP workloads** - много вставок и простых запросов
2. **Document-oriented** приложений - работа с документами целиком
3. **Low-latency** требований - минимальные задержки
4. **Small schemas** - мало полей, простая структура

#### Используйте **document_table** для:
1. **OLAP workloads** - аналитические запросы
2. **Wide schemas** - много полей, сложная структура
3. **Projection queries** - выборка малого числа колонок
4. **Aggregations** - COUNT, SUM, AVG и т.д.
5. **Dynamic schemas** - частые изменения структуры
6. **Primary key lookups** - с нашим primary_key_scan (O(1))

### Что нужно улучшить в document_table

1. **Оптимизация INSERT**:
   - Кэширование результатов schema evolution
   - Batch processing для множественных вставок
   - Lazy materialization колонок

2. **Оптимизация SELECT**:
   - Projection pushdown (читать только нужные колонки)
   - Векторизованное выполнение
   - Late materialization (собирать документы только когда нужно)

3. **Индексы**:
   - Secondary indexes на часто используемые колонки
   - Zone maps для быстрой фильтрации
   - Bloom filters для existence checks

---

## 🎉 Достижения реализации primary_key_scan

Несмотря на то, что document_table медленнее на simple queries, наш **primary_key_scan** показал:

1. ✅ **O(1) lookup по _id** вместо O(N)
2. ✅ **33.6x ускорение** на 10K документах (1ms vs 37ms)
3. ✅ **Корректная работа** - все 63 теста пройдены
4. ✅ **Использует id_to_row_ map** - эффективная реализация

**primary_key_scan делает document_table конкурентным для mixed workloads!**

---

## 🏁 Заключение

### Что мы узнали о проблеме document storage:

1. **Причина**: `get_document_id()` не генерирует _id автоматически
2. **Эффект**: все документы без _id получают одинаковый ID
3. **Результат**: в B-tree сохраняется только последний документ
4. **Решение**: явная генерация уникального _id перед вставкой

### Текущее состояние производительности:

- **document (B-tree)**: 🏆 **Чемпион для simple queries** (49-95x быстрее)
- **document_table**: 🎯 **Специалист для analytics** (потенциал 10-50x для OLAP)

### Что дальше:

1. Исправить генерацию _id в document storage (чтобы работало автоматически)
2. Оптимизировать document_table для projection queries
3. Добавить secondary indexes для оптимизации WHERE
4. Реализовать vectorized execution для analytical queries
5. Benchmark на больших датасетах (10K+, 100K+, 1M+)

---

**Вывод:** Обе системы имеют свои ниши. document (B-tree) отлично работает для transactional workloads, а document_table (с нашим primary_key_scan) — это отличный фундамент для hybrid transactional/analytical processing (HTAP).

